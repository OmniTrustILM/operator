# Platform versions — deploying a specific ILM release

This guide has moved into the user guide under [`docs/site/`](./site/overview.md), as
[Upgrading](./site/upgrading.md) — the
[supported version matrix](./site/upgrading.md#supported-versions), how `spec.version` is
resolved, and the supported-range model now sit alongside the upgrade procedure that used
to live in `upgrades.md`.

That page is published at https://docs.otilm.com.
