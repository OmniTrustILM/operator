# ILM Platform — configuration reference & scenario cookbook

This guide has moved into the user guide under [`docs/site/`](./site/overview.md), where it was
split in two. What each option means, the scenarios, and which sample fits which need are now
[The Platform CR](./site/custom-resources/platform.md). The complete option index — every
`spec` field, its default, and what it is for — is
[Platform options](./site/custom-resources/platform-options.md).

Both pages are published at https://docs.otilm.com.
